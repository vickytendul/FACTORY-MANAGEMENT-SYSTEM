{
  // Template only - copy to appsettings.Development.json and add your own
  // secrets there. That file is gitignored and must never be committed.
  // Supabase:ConnectionString goes in YOUR copy, not here.
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=FactoryManagementSystem;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=True"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
